# Weccoo (Frontend)

Frontend web de Weccoo (Vite + React).

## Démarrer (local)

1. `npm install`
2. (Optionnel) Créer `.env` :
   - `VITE_API_BASE_URL=http://localhost:4000/api`
3. `npm run dev`

App : `http://localhost:3000`
